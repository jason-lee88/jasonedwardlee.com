import { DefaultTheme } from "styled-components";
import colors from "./colors";

const DarkTheme: DefaultTheme = {
  colors: {
    background: colors.navy,
    theme: colors.blue,
    typography: colors.faded_blue,
    heading: colors.off_white,
  },
};

export default DarkTheme;
